/**
 * Plan reviewer module.
 *
 * Phase 2 fills this in: before_agent_start prompt injection plus the
 * present_plan tool that renders a plan presentation and opens it in Chrome.
 */

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export function registerPlanReview(pi: ExtensionAPI): void {
  void pi;
}
